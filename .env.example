# Public API origin for production (Netlify → QNAP NAS / server).
# Example: https://api.your-nas.example.com
VITE_API_URL=

# Dev-only: Vite dev-server proxy target (see vite.config.ts)
VITE_API_PROXY_TARGET=http://localhost:8001
