# Production API origin (Netlify → Render). No trailing slash.
# Example: https://your-service.onrender.com
VITE_API_URL=

# Dev-only: Vite dev-server proxy target (see vite.config.ts)
VITE_API_PROXY_TARGET=http://localhost:8001
